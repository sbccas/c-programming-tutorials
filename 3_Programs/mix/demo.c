/* REMARK STATEMENT

 COMMENT*/
#include <stdio.h>
#include <conio.h>
int main()
{
 char sname[50];
 char coption;
 //clrscr();
 //gets(sname);
 coption = getchar();
 //puts(sname);
 putchar(coption);
getch();
 return 0;
}